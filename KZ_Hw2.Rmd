---
title: "Hw02_KZ"
output: github_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


This is my 2nd homework assignemtn for STAT 545 

In this assignemnt I will be exploring Gapmindeer and using dplyr
 
```{r}
library(gapminder)
```
 
```{r}
library(tidyverse)
```
 
```{r}
?gapminder
```

When I ask R about Gapminder it tell me that Gapminder is an "excerpt of the Gapminder data on life expectancy, GDP per capita, and population by country".

```{r}
typeof(gapminder)
```

```{r}
class(gapminder)
```

```{r}
mode(gapminder)
```

length(gapminder)
dim(gapminder)
typeof(gapminder$country)

head(gapminder)
str(gapminder)
summary(gapminder)

range(year)
lenght(year)
summary(year)

what type of data is each variable 
typeof(gapminder$country)
typeof(gapminder$gdpPercap)
typeof(gapminder$continent)
typeof(gapminder$year)
typeof(gapminder$pop)
typeof(gapminder$lifeExp)


